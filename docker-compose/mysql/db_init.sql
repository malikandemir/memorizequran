CREATE TABLE `travellist`.`memorized_pages` (
                                                `id` INT NOT NULL AUTO_INCREMENT,
                                                `page_number` INT NULL,
                                                `status` INT NULL,
                                                `created_at` DATETIME NULL,
                                                `updated_at` DATETIME NULL,
                                                PRIMARY KEY (`id`));
